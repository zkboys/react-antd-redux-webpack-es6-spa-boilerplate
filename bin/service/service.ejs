import BaseService from '../base-service';

export default class <%= capitalName %>Service extends BaseService {
    constructor(...args) {
        super(...args);
        this.url = '<%= ajaxUrl %>';
    }
}
